# Template variabel untuk frontend (local dev & staging).
# Copy jadi .env (local) atau .env.dev (staging), lalu isi value asli.
# JANGAN commit .env / .env.dev yang sudah berisi value asli.

VITE_API_BACKEND=
VITE_API_WEBSOCKET=
VITE_PUBLIC_BUILDER_KEY=
